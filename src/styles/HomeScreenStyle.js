import { StyleSheet } from "react-native"
const styles = StyleSheet.create({
    MainTouchable: {
        marginVertical: 20,
        paddingVertical :50,
        alignItems : "center"
    },
    MainTouchableText: {
        fontWeight : "bold",
        fontSize : 20
    },
    MainTouchableButton: {
        backgroundColor : "gold",
        padding:10,
        marginVertical : 11,
        borderRadius : 10
    },
    MainTouchableButtonText: {
        
    }
});
export default styles;